# Atoms Demo 笔试说明文档

## 一、项目概述

**项目名称**：Atoms Demo - AI 驱动的全栈应用生成平台

**一句话介绍**：用户通过自然语言描述需求，多智能体协作自动生成可运行的全栈 Web 应用，并在浏览器中实时预览。

**参考原型**：atoms.dev

---

## 二、技术栈与架构选型

### 前端
- **框架**：Next.js 14 App Router + TypeScript
- **UI 组件**：TailwindCSS + shadcn/ui
- **状态管理**：React Hooks + SWR
- **实时通信**：SSE (Server-Sent Events) 流式输出
- **代码预览**：iframe 沙箱 + Blob URL
- **认证**：Supabase Auth（邮箱密码）

### 后端 Agent
- **Web 框架**：FastAPI + uvicorn
- **Agent 编排**：LangGraph（五节点串行工作流）
- **LLM**：DeepSeek API（流式输出）
- **任务调度**：数据库轮询模式 + 乐观锁抢占

### 数据层
- **数据库**：Supabase PostgreSQL（RLS 行级安全）
- **对象存储**：Supabase Storage（部署产物托管）
- **认证**：Supabase Auth

---

## 三、核心工作流设计

采用五阶段 Agent 工作流，模拟真实开发流程：

```
用户输入需求
    ↓
[Analyze] 需求分析师 - 拆解功能点列表
    ↓
暂停 → 用户确认功能点
    ↓
[Select Template] 模板匹配器 - 匹配基础全栈模板
    ↓
[Design] 架构师 - 输出技术方案 + File Tree Plan
    ↓
[Code] 工程师 - 按文件树增量生成多文件代码
    ↓
[Deploy] 部署员 - 构建并上传到 Storage
    ↓
暂停 → 用户预览确认 / 继续迭代修改
```

**设计理念**：
- 架构先行（File Tree Plan），避免 AI 编码跑偏
- 模板驱动 + 增量生成，保证基础设施代码质量
- 人类介入两个关键决策点（功能确认 / 结果确认）

---

## 四、已实现功能

### ✅ 核心流程
1. **用户系统**：注册、登录、登出、路由守卫
2. **项目管理**：新建、删除、项目列表
3. **对话式生成**：左侧对话区 + 右侧预览区的经典布局
4. **真流式输出**：LLM 逐 token 通过 SSE 推送，前端打字机效果
5. **步骤进度条**：分析→设计→编码→部署 四步可视化
6. **功能点确认环节**：AI 拆解功能点，用户勾选确认后继续
7. **迭代修改**：确认不满意可继续对话迭代
8. **多文件代码生成**：按 ```file:路径 格式输出多文件

### ✅ 预览与部署
1. **预览区 Tab 切换**：效果预览 / 代码编辑器（含文件树）
2. **设备切换**：桌面 / 平板 / 手机三档预览
3. **fullstack 模板构建部署**：npm install + vite build + 注入 Mock API + 上传 Storage
4. **多分辨率适配**：预览区工具栏支持设备尺寸切换

### ✅ 工程化
1. **断点续跑**：每步结果持久化到数据库，重启不丢失
2. **任务幂等**：同一项目同时只跑一个任务
3. **容错降级**：构建失败自动 fallback 到错误提示页
4. **RLS 安全**：用户只能访问自己的项目数据

---

## 五、关键技术取舍

| 决策 | 选择 | 理由 |
|------|------|------|
| Agent 编排 | LangGraph 极简串行 | MVP 阶段控制复杂度，后续可扩展为 DAG |
| 任务调度 | 数据库轮询 | 避免引入 Redis/消息队列，部署简单 |
| 生成模式 | 模板驱动 + 增量 | 纯 AI 生成基础设施代码质量不稳定，模板保底 |
| 前端预览 | iframe 沙箱 | 安全隔离生成的代码，避免污染主页面 |
| 数据库 | Supabase | 一站式搞定 DB/Auth/Storage，MVP 阶段效率最高 |
| 流式方案 | SSE | 比 WebSocket 简单，满足单向流式输出需求 |

---

## 六、当前已知问题与限制

1. **Supabase 美国节点网络波动**：国内访问偶发连接断开，已加重试容错
2. **fullstack 部署 Mock 数据较简单**：API Mock 只返回假数据，不支持真实业务逻辑
3. **代码生成质量依赖 LLM**：复杂项目可能需要多轮迭代修正
4. **编辑器功能较基础**：当前是 textarea 实现，未接入 Monaco Editor
5. **Windows 环境构建警告**：GBK 编码输出警告，不影响功能
6. **Storage Content-Type 设置**：Python SDK 参数兼容性问题，已通过双保险方案修复

---

## 七、后续扩展方向（优先级排序）

### P0 - 核心体验优化
1. **代码编辑器升级**：接入 Monaco Editor，支持语法高亮、多标签编辑
2. **生成进度可视化**：逐文件生成进度、任务清单 checkbox 打勾
3. **迭代增量修改**：基于已有文件 diff 修改，而非全量重写
4. **架构文档可视化展示**：设计阶段输出结构化文档，友好渲染

### P1 - 功能增强
1. **项目导出 ZIP**：一键下载完整项目代码
2. **模板市场**：多套模板可选（纯前端 / 全栈 / 移动端 H5）
3. **用户编辑同步预览**：编辑器改完代码实时刷新预览
4. **分享部署链接**：生成公开分享链接

### P2 - 架构升级
1. **引入代码质量检查**：生成后自动 lint / type check / build 校验
2. **评审 Agent 节点**：新增 Reviewer 节点自检代码质量
3. **真实后端部署**：从 Mock 部署升级为真实 Serverless 函数部署
4. **任务队列升级**：轮询换 Celery/Redis，支持优先级、并发控制

### P3 - 产品化
1. **多模态输入**：支持截图、参考图生成页面
2. **组件库扩展**：接入更多 UI 库模板（Ant Design、MUI 等）
3. **团队协作**：多人共享项目、评论功能
4. **用量计费系统**：按生成次数/Token 计费

---

## 八、项目链接

- **GitHub 仓库**：https://github.com/mwh1233/atoms-demo
- **本地运行**：
  - 前端：`cd frontend && npm install && npm run dev`
  - 后端：`cd backend && pip install -r requirements.txt && python -m uvicorn app.main:app --reload`
- **环境变量**：需配置 Supabase 项目密钥和 DeepSeek API Key

---

## 九、开发时间投入

约 12 小时专注开发，完成核心架构、Agent 工作流、前后端联调、部署链路打通。
