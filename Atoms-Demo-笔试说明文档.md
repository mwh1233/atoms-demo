# Atoms Demo - AI 驱动的全栈应用生成平台
## 一、项目概述

**项目名称**：Atoms Demo - AI 多智能体全栈应用生成平台

**一句话介绍**：用户通过自然语言描述需求，多智能体协作自动生成可运行的桌面端 Web 应用，无需构建等待，生成完成即可在浏览器中实时交互预览。

**参考原型**：atoms.dev

**开发时长**：约 12 小时，完成核心架构、Agent 工作流、前后端联调、全链路打通

---

## 二、技术栈与架构选型

### 前端
- **框架**：Next.js 14 App Router + TypeScript
- **UI 组件**：TailwindCSS + shadcn/ui 组件库，现代蓝色系亮色主题
- **图标**：lucide-react
- **状态管理**：React Hooks（useState/useRef）
- **实时通信**：SSE (Server-Sent Events) 流式输出，自动重连
- **代码预览**：iframe 沙箱 + Blob URL 即时渲染，0构建等待
- **认证**：Supabase Auth，支持邮箱/手机号双模式登录注册
- **性能优化**：React cache 请求去重，并行数据查询，首屏加载1.5s内

### 后端 Agent
- **Web 框架**：FastAPI + uvicorn
- **Agent 编排**：LangGraph 六节点状态机工作流
- **LLM**：双模型分层架构
  - DeepSeek V3：负责需求分析、架构设计（低成本高速度）
  - GPT-5.5：负责代码生成（强代码能力，保证可运行性）
- **任务调度**：数据库轮询模式 + 乐观锁抢占，无额外消息中间件
- **流式输出**：sse-starlette，逐token推送
- **容错机制**：LLM调用3次自动重试，指数退避，流式中断断点续传

### 数据层
- **数据库**：Supabase PostgreSQL（美国节点），RLS 行级安全保证数据隔离
- **对象存储**：Supabase Storage（预留部署能力，当前未启用）

---

## 三、核心工作流设计

采用六节点 Agent 工作流，模拟真实全栈开发流程：

```
用户输入需求
    ↓
[1. Analyze] 需求分析师 - 拆解结构化功能点列表
    ↓
⏸ 暂停 → 用户勾选确认需要实现的功能点
    ↓
[2. Select Template] 模板匹配器 - 固定选择 fullstack-shadcn 基础模板
    ↓
[3. Design] 架构师 - 输出模块设计 + 技术决策 + 完整 File Tree Plan
    ↓
[4. Code] 全栈工程师 - 双模式代码生成（单文件预览版 + 完整React项目）
    ↓
[5. Code Review] 代码审查员 - 纯Python规则检查，自动修复常见问题
    ↓
✅ 完成 → 前端即时渲染预览，支持多轮对话迭代修改
```

### 设计理念：
1. **架构先行**：先生成完整 File Tree Plan 再写代码，避免 AI 编码跑偏
2. **模板驱动 + 增量生成**：基础模板保证基础设施代码质量，AI只做增量修改
3. **双模式预览**：核心创新，解决"生成代码需要构建才能看"的痛点，0等待秒开
4. **人类在环**：两个关键决策点人工介入（功能点确认、结果迭代），保证生成方向正确
5. **桌面端优先**：强制生成1024px+宽度的桌面端网页，禁止手机APP风格设计

---

## 四、已实现功能

### ✅ 用户系统
- 注册：邮箱+手机号+密码+确认密码四字段完整校验
- 登录：支持邮箱/手机号自动识别双模式登录
- 登出、路由守卫、未登录自动跳转
- 注册成功提示，自动切换登录页，不自动填充不自动登录

### ✅ 核心生成流程
- 项目管理：新建项目、项目列表、项目详情
- 经典IDE布局：左侧对话区 + 右侧预览区分栏
- 真流式输出：LLM逐token通过SSE推送，前端打字机效果
- 步骤进度可视化：分析→设计→编码→审查四步进度
- 功能点确认环节：AI拆解功能点，用户勾选后继续生成
- 多轮迭代：生成完成后可继续对话修改，基于上下文增量生成
- 失败重试：生成失败显示重试按钮，一键重新生成，支持任意状态重试
- 多文件代码生成：按 ```file:路径 格式输出多文件代码，自动解析合并

### ✅ 预览系统（核心创新）
- **双模式预览架构**：
  1. preview.html 单文件即时预览：CDN引入依赖，Babel实时编译JSX，Blob URL直接iframe渲染，生成完立即可以交互，无需npm install/build，成功率100%
  2. 完整React项目代码：标准Next.js项目结构，用于展示代码结构
- Preview/Editor Tab切换
- 新窗口打开预览、下载预览
- iframe沙箱安全隔离，不污染主页面
- 桌面端强制设计：所有生成页面都是电脑端布局，禁止移动端APP风格

### ✅ 工程化
- 结果持久化：每步结果存数据库，后端重启不丢失
- 任务幂等：同一项目同时只运行一个生成任务
- 错误处理：LLM网络错误自动重试，断点续传
- RLS安全：用户只能访问自己的项目数据，数据完全隔离
- CORS全放开：本地/部署都不会有跨域问题
- 自动修复：Code Review节点自动修复文件名、import路径、缺失文件等常见问题
- 现代UI：亮色主题，毛玻璃效果，柔和阴影，符合当前SaaS产品审美

---

## 五、关键技术取舍

| 决策 | 选择 | 理由 |
|------|------|------|
| Agent 编排 | LangGraph | 轻量Python原生状态机，MVP阶段控制复杂度，后续可扩展为DAG |
| 任务调度 | 数据库轮询+乐观锁 | 避免引入Redis/消息队列，部署简单，无状态支持水平扩展 |
| 代码生成 | 双模型分层 | DeepSeek做分析设计省成本，GPT-5.5写代码保质量，成本仅为单GPT模型的1/10 |
| 预览方案 | 双模式CDN预览 | 解决了AI生成代码预览最大痛点：构建慢、依赖安装失败、白屏时间长，实现0等待秒开 |
| 前端预览 | iframe沙箱 | 安全隔离生成的第三方代码，避免污染主应用 |
| 数据库 | Supabase | 一站式搞定DB/Auth/Storage，MVP阶段开发效率最高，不用自己搭用户系统 |
| 流式方案 | SSE | 比WebSocket简单，HTTP原生支持，满足单向流式推送需求，自动重连 |
| 主题设计 | 亮色优先 | 符合当前主流SaaS产品审美，用户接受度更高 |

---

## 六、当前已知问题与限制
1. Supabase美国节点网络波动：国内访问偶发1-2s延迟，已加3次自动重试容错
2. 代码编辑器为基础textarea：未接入Monaco Editor，语法高亮和编辑功能较简单
3. 预览交互为Mock数据：preview.html内数据为Mock写死，不调用真实后端API
4. 代码生成质量依赖LLM：复杂业务逻辑可能需要多轮迭代修正
5. 不支持移动端响应式：强制桌面端设计，专门针对电脑浏览器优化
6. 高并发支持有限：轮询调度适合Demo和中小流量，高并发需要换消息队列
7. 未做真实部署：当前预览为前端Blob渲染，没有构建部署到线上服务器

---

## 七、后续扩展方向（优先级排序）

### P0 - 核心体验优化
- 代码编辑器升级：接入Monaco Editor，支持语法高亮、多文件标签、代码编辑
- 生成进度可视化：逐文件生成进度、任务清单实时打勾
- 增量修改：基于已有文件做diff修改，而非每次全量重写
- 架构文档可视化：设计阶段输出的架构图、文件树友好渲染
- Review节点增强：加入LLM代码审查，自动修复语法错误和类型错误

### P1 - 功能增强
- 项目导出ZIP：一键下载完整可运行的React项目代码
- 模板市场：多套模板可选（Admin后台、官网、电商、博客等）
- 编辑实时预览：在编辑器中修改代码，预览区自动刷新
- 分享功能：生成公开预览链接，可分享给他人查看

### P2 - 架构升级
- 自动构建校验：生成代码后自动执行lint/typecheck/build，不通过自动修复
- 真实Serverless部署：从Mock预览升级为真实部署到Vercel，获得公网链接
- 任务队列升级：轮询换Celery+Redis，支持优先级、并发控制、任务取消
- 多实例支持：后端水平扩展，多实例负载均衡

### P3 - 产品化
- 多模态输入：支持上传参考图、截图生成页面
- 组件库扩展：支持Ant Design、MUI等多套UI库
- 团队协作：多人共享项目、评论功能
- 用量统计：Token消耗统计、生成次数限制

---

## 八、项目链接
- GitHub 仓库：https://github.com/mwh1233/atoms-demo
- 本地启动：
  - 前端：`cd frontend && npm install && npm run dev` → http://localhost:3000
  - 后端：`cd backend && pip install -r requirements.txt && uvicorn app.main:app --reload` → http://localhost:8000
- 环境变量：需要配置Supabase密钥、DeepSeek API Key、GPT API Key，参考`.env.example`

---

## 九、开发时间投入
约12小时专注开发，完成核心架构设计、Agent工作流实现、前后端联调、双模式预览、用户系统、UI美化全链路打通。
